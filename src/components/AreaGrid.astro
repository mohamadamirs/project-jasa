---
import SectionHeader from './SectionHeader.astro';
import AreaMarquee from './react/AreaMarquee';

interface Area {
  id: string;
  data: {
    cityName: string;
  };
}

interface Props {
  areas: Area[];
  title?: string;
  subtitle?: string;
  class?: string;
}

const { 
  areas, 
  title = "Kami Hadir di Dekat Anda", 
  subtitle = "Klik wilayah Anda untuk melihat penawaran khusus dan konsultasi personal.",
  class: className 
} = Astro.props;
---

<section class:list={["section-spacing bg-white overflow-hidden", className]}>
  <div class="container-custom">
    <div class="text-center max-w-2xl mx-auto">
      <SectionHeader {title} {subtitle} align="center" />
    </div>

    <!-- Mobile Island: React Marquee with Focus-Zoom -->
    <div class="md:hidden">
      <AreaMarquee {areas} client:load />
    </div>

    <!-- Desktop View: Static Grid (SEO Friendly & Fast) -->
    <div class="hidden md:grid grid-cols-5 gap-6">
      {areas.map((area) => (
        <a href={`/${area.id}/`} class="group p-8 bg-white rounded-[2rem] border border-slate-100 hover:bg-blue-600 transition-all duration-500 ease-out text-center hover:shadow-xl hover:shadow-blue-200 hover-lift active:scale-95 flex flex-col items-center justify-center min-h-[160px]">
          <h4 class="w-full text-slate-900 group-hover:text-white transition-colors duration-300 leading-tight font-bold">{area.data.cityName}</h4>
          <span class="text-caption text-slate-400 group-hover:text-blue-100 transition-colors duration-300 flex items-center justify-center mt-4">
            Detail
          </span>
        </a>
      ))}
    </div>
  </div>
</section>
